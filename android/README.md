Android host files for the INET Flutter starter.

This starter originally ships without a generated Android host project.
If you are wiring this into a fresh clone, run:

flutter create . --platforms=android,ios

Then keep the files in this folder and adjust the Kotlin package name if your applicationId is not `com.example.inet_app`.
